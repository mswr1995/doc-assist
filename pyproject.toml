[project]
name = "doc-assist"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pypdf",
    "python-docx",
    "chromadb",
    "pytest",
    "ollama"
]
